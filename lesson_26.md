<h1 align="center" style="color:#2E86C1;">R'SOFT</h1>
<p align="center" style="color:#2E86C1; font-size:20px;">Web Development Company</p>

---

## 🧩 Lesson 26 — Modules

---

### ✳️ Exercise 1 — Օգտագործել math մոդուլը թիվը կլորացնելու համար

Գրել ֆունկցիա, որը ստանում է `float` թիվ որպես պարամետր։
և `math.ceil()` ու `math.floor()` օգտագործելով տպում է վերև և ներքև կլորացված արժեքները։

---

### ✳️ Exercise 2 — Օգտագործել math մոդուլը թիվը կլորացնելու համար

Exercise 1֊ի շարունակությունը:  
Գրել ֆունկցիա, որը ստանում է `float` թիվ որպես առաջի պարամետր, իսկ երկրորդ պարամետր ընդունում է `boolean`։

- Եթե երկրորդ պարամետրը True է, ապա՝ կլորացնում է `math.ceil()`֊ի միջոցով և `return` է անում արժեքը։
- Եթե երկրորդ պարամետրը True է, ապա՝ կլորացնում է `math.floor()`֊ի միջոցով և `return` է անում արժեքը։

---

### ✳️ Exercise 3 — Օգտագործել random.randint()

Գրել ծրագիր, որը տպում է 1-ից 100 պատահական թիվ։

---

### ✳️ Exercise 4 — random.choice()

Ստեղծել մարդկանց անունների `list` և տպել պատահական անուն։

---

### ✳️ Exercise 5 — datetime — այսօր

Տպել այսօրվա ամսաթիվը `datetime.datetime.today()` օգտագործելով։

---

### ✳️ Exercise 6 — time.sleep()

Գրել ծրագիր, որը տպում է 1…5 թվերը՝ մեկ վայրկյան ընդմիջումով։

---

### ✳️ Exercise 7 — Սեփական Module

1. Ստեղծել սեփական մոդուլ `"custom_functions.py"` անունով։
2. Որը կունենա իր մեջ 3 ֆունկցիա `x`, `y`, `z`
3. Ամեն ֆունկցիա իր մեջ `print` կանի համապատասխանաբար՝ `"Hello, I am X"`, `"Hello, I am Y"`, `"Hello, I am Z"`
4. Ստեղծիր առանձին ֆաիլ՝ `"main_app.py"` անունով
5. `"main_app.py"` ֆաիլում `import` արա `"custom_functions.py"` մոդուլը
6. `"main_app.py"` ֆաիլում օգտագործիր `x`, `y`, `z` ֆունկցիաները


