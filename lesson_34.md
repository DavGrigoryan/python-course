<h1 align="center" style="color:#2E86C1;">R'SOFT</h1>
<p align="center" style="color:#2E86C1; font-size:20px;">Web Development Company</p>

---

# 🧩 Lesson 34 – Object-Oriented Programming (OOP) – Part 5

## OOP 4 Principles

- INHERITANCE
- ENCAPSULATION
- ABSTRACTION
- POLYMORPHISM

## 👉 POLYMORPHISM (բազմաձևություն)

Ունի երկու տեսակ`

- Override (Վերասահմանում)
- Overload (Գերբեռնում)

---

### Վերասահմանում (Override)

**Օրինակ՝**

```python
class Animal:
    def make_sound(self):
        # Ծնողի Ընդհանուր Վարքագիծը
        return "Unknown Sound"


class Dog(Animal):
    # Վերասահմանում. Փոխում ենք ծնողի վարքագիծը
    def make_sound(self):
        return "Woof! (Overridden)"


class Cat(Animal):
    # Վերասահմանում. Փոխում ենք ծնողի վարքագիծը
    def make_sound(self):
        return "Meow! (Overridden)"


# Բազմաձևություն
animals = [Animal(), Dog(), Cat()]
for a in animals:
    # Կանչվում է ճիշտ տարբերակը (Dog-ի կամ Cat-ի տարբերակը)
    print(a.make_sound())
```

### Գերբեռնում (Overloading)

Մեթոդի գերբեռնումը նշանակում է, որ դասի ներսում կարող եք ունենալ մի քանի մեթոդներ՝ նույն անունով,   
բայց տարբեր պարամետրերով (օրինակ՝ տարբեր քանակությամբ կամ տեսակներով)։

Այստեղ կա շատ կարևոր նրբերանգ`

- **Python-ը ՉԻ Աջակցում** ավանդական մեթոդի գերբեռնմանը (Overloading), ինչպես Java-ն կամ C#-ը։
  Եթե դուք դասի ներսում հայտարարեք նույն անունով երկու մեթոդ, Python-ը կպահի **միայն վերջին** հայտարարվածը։
- Փոխարենը, Python-ը հասնում է նույն արդյունքին (բազմաձևության)՝ օգտագործելով:
    - Լռելյայն արժեքներ (Default values):
    - Փոփոխական պարամետրեր (`*args` և `**kwargs`):

**Տարբերակ 1 օրինակ՝**

```python
class Calculator:

    # Գերբեռնման Համարժեքը՝ Լռելյայն Արժեքներով
    def add(self, a, b=None, c=None):
        """
        Միակ մեթոդը, որը կատարում է 2 կամ 3 թվերի գումարում։
        """
        if b is None and c is None:
            # Եթե տրված է միայն մեկ թիվ
            return a
        elif c is None:
            # Եթե տրված է երկու թիվ
            return a + b
        else:
            # Եթե տրված է երեք թիվ
            return a + b + c


# Օգտագործումը (Բազմաձևություն)
calc = Calculator()

# Կանչ 1 (Համարժեք է add(a))
print(f"Մեկ թիվ. {calc.add(10)}")  # Արդյունքը՝ 10

# Կանչ 2 (Համարժեք է add(a, b))
print(f"Երկու թիվ. {calc.add(10, 20)}")  # Արդյունքը՝ 30

# Կանչ 3 (Համարժեք է add(a, b, c))
print(f"Երեք թիվ. {calc.add(10, 20, 30)}")  # Արդյունքը՝ 60
```

**Տարբերակ 2 օրինակ՝**

```python
class Calculator:

    # Գերբեռնման Ավելի Լավ Համարժեքը՝ Օգտագործելով *args
    def add(self, *numbers):
        """
        Հաշվարկում է ցանկացած քանակությամբ թվերի գումարը։
        
        :param numbers: Թվերի tuple (*args-ի միջոցով)
        :return: Բոլոր թվերի գումարը
        """

        # 1. Դատարկ մուտքի ստուգում (եթե ցանկալի է)
        if not numbers:
            return 0  # Կամ կարող եք բարձրացնել ValueError

        # 2. Գումարում (օգտագործելով Python-ի ներկառուցված sum() ֆունկցիան)
        return sum(numbers)


# ----------------- Օգտագործումը -----------------
calc = Calculator()

print("### Օգտագործումը *args-ով ###")

# Կանչ 1 (Զրո թիվ)
print(f"Զրո թիվ. {calc.add()}")

# Կանչ 2 (Մեկ թիվ)
print(f"Մեկ թիվ. {calc.add(10)}")

# Կանչ 3 (Երկու թիվ)
print(f"Երկու թիվ. {calc.add(10, 20)}")

# Կանչ 4 (Հինգ թիվ) - Ավելի ճկուն
print(f"Հինգ թիվ. {calc.add(1, 2, 3, 4, 5)}")
```

**Տարբերակ 3 օրինակ՝**

```python
class Number:
    def __init__(self, value):
        self.value = value

    def __add__(self, other):
        return self.value + other.value


a = Number(5)
b = Number(7)

print(a + b)  # 12
```

# Exercise 1

## Ստեղծեք `Bird`, `Fish`, `Dog` clas-ները յուրաքանչյուրը `move()` մեթոդով

- Bird(Ծիտիկը) → "flies" (թռնել)
- Fish(Ձուկ) → "swims" (լողալ)
- Dog(Շուն) → "runs" (վազել)
- Տեղադրեք դրանք list-ի մեջ և ցիկլով կանչեք `.move()` ֆունկցիան։

---

# Վարժություն 2 – Տրանսպորտի շարժումը

Առաջադրանք

Ստեղծեք `Transport` ծնող class, որտեղ կա `move()` մեթոդը, որը վերադարձնում է `"Unknown move"`։

Ստեղծեք 3 ժառանգված դասեր՝

- `Car` → `move()` → `"drives"`

- `Bike` → `move()` → `"rides"`

- `Boat` → `move()` → `"sails"`

Այնուհետև

- Ստեղծեք list այս օբյեկտներով
- Ցիկլով անցեք list-ի վրայով և տպեք obj.move() արդյունքը։

---

## Վարժություն 3 – Գումարում args-ով

Առաջադրանք

Ստեղծեք `MathOps` class, որտեղ կլինի `multiply(*nums)` մեթոդը.

Պահանջներ․

- Եթե ոչ մի թիվ չի տրվում → վերադարձնել `1`
- Եթե տրվում է `1` թիվ → վերադարձնել այդ թիվը
- Եթե տրվում է `2` կամ ավելի թվեր → վերադարձնել բոլոր թվերի արտադրյալը
- Օգտագործել միայն `*args`, ոչ մի default value

Օրինակ կանչեր՝

- `multiply()` → `1`
- `multiply(7)` → `7`
- `multiply(2,3,4)` → `24`

---

## Վարժություն 4 – Տպել օգտվողի տվյալները kwargs-ով

Առաջադրանք

Ստեղծել `UserPrinter` class, որտեղ կա մեթոդ՝
`print_info(**kwargs)`

Պետք է վերադարձնի string այս ձևաչափով՝

```python
key1 = value1
key2 = value2
key3 = value3
```

Օրինակ կանչ․
`print_info(name="Hayk", age=25)` →

```python
name = Hayk
age = 25
```

Պահանջ․ օգտագործել միայն `**kwargs`։
