<h1 align="center" style="color:#2E86C1;">R'SOFT</h1>
<p align="center" style="color:#2E86C1; font-size:20px;">Web Development Company</p>

---

# 🧩 Lesson 36 – Virtual Environment

Ի՞նչ է վիրտուալ միջավայրը (Virtual Environment):
Python-ի վիրտուալ միջավայրը (virtual environment) ձեր համակարգչի վրա մեկուսացված միջավայր է, որտեղ դուք կարող եք
գործարկել և փորձարկել ձեր Python նախագծերը։
իրտուալ միջավայրը մտածեք որպես առանձին կոնտեյներ (container) յուրաքանչյուր Python նախագծի համար։

---

### Creating a Virtual Environment

Python-ն ունի ներկառուցված (`built-in`) `venv` մոդուլը՝ վիրտուալ միջավայրեր ստեղծելու համար:  
Ձեր համակարգչի վրա վիրտուալ միջավայր ստեղծելու համար բացեք `terminal`֊ը անցեք այն թղթապանակը,
որտեղ ցանկանում եք ստեղծել ձեր նախագիծը, ապա մուտքագրեք այս հրամանը.

#### Օրինակ՝

Աշխատացրեք այս հրամանը `myfirstproject` անունով վիրտուալ միջավայրեր ստեղծելու համար՝

```shell
python -m venv myfirstproject
```

Սա կկարգավորի վիրտուալ միջավայր և կստեղծի "myfirstproject" անունով թղթապանակ՝ ենթաթղթապանակներով և ֆայլերով, ինչպես այս
օրինակում է.

### Արդյունքը՝

Ֆայլի/թղթապանակի կառուցվածքը կունենա հետևյալ տեսքը.

```text
myfirstproject
  Include
  Lib
  Scripts
  .gitignore
  pyvenv.cfg
```

---

### Ակտիվացնել Վիրտուալ Միջավայրը (Activate Virtual Environment)

Վիրտուալ միջավայրը օգտագործելու համար դուք պետք է այն ակտիվացնեք այս հրամանով:

#### Օրինակ՝

Ակտիվացրեք վիրտուալ միջավայրը.

**Windows-ի համար (For Windows):**

```shell

myfirstproject\Scripts\activate
```

**macOS/Linux-ի համար (For macOS/Linux):**

```shell

source myfirstproject/bin/activate
```

#### Արդյունքը

`command line`֊ը այս տեսքը կունենա, երբ վիրտուալ միջավայրը ակտիվ է.

**Windows:**

```shell

(myfirstproject) C:\Users\Your Name>
```

**macOS/Linux:**

```shell

(myfirstproject) ... $
```

---

### Փաթեթների Տեղադրում (Install Packages)

Երբ ձեր վիրտուալ միջավայրը ակտիվացված է, դուք կարող եք նրանում փաթեթներ տեղադրել՝ օգտագործելով `pip`-ը:

Մենք կտեղադրենք 'cowsay' կոչվող փաթեթը.

#### Օրինակ՝

Տեղադրեք 'cowsay'-ը վիրտուալ միջավայրում.

```shell

pip install cowsay
```

---

### Ապաակտիվացնել Վիրտուալ Միջավայրը (Deactivate Virtual Environment)

Վիրտուալ միջավայրն ապաակտիվացնելու համար օգտագործեք այս հրամանը.

#### Օրինակ՝

Ապաակտիվացրեք վիրտուալ միջավայրը `terminal`-ում.

```shell

deactivate
```

---

### Ջնջել Վիրտուալ Միջավայրը (Delete Virtual Environment)

Վիրտուալ միջավայրով աշխատելու մեկ այլ առավելությունն այն է, որ երբ դուք ինչ-որ պատճառով ցանկանում եք այն ջնջել,
այլ նախագծեր դրանից կախված չեն, և ջնջվում են միայն նշված վիրտուալ միջավայրի մոդուլներն ու ֆայլերը:

#### Օրինակ՝

Delete myfirstproject from the command line interface:
Ջնջել myfirstproject-ը `command line` ինտերֆեյսից (CLI).

**Windows-ի համար:**

```shell

rmdir /s /q myfirstproject
```

**macOS/Linux-ի համար:**

```shell

rm -rf myfirstproject
```
